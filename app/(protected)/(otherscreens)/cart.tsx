import React from 'react';

import { Cart } from '~/components/Cart';
import { Wrapper } from '~/components/ui/Wrapper';

const cart = () => {
  return (
    <Wrapper>
      <Cart />
    </Wrapper>
  );
};

export default cart;
